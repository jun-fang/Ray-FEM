global omega;
pde = Helmholtz_data1;
NPW = 6;
a = 1/

omegas = [10 20 40 80]*pi;
errors = 0*omegas;
for i = 1:length(omegas)
    omega = omegas(i);
    h = 1/(NPW*round(omega/(2*pi)));
    [node,elem] = squaremesh([-a,a,-a,a],h);