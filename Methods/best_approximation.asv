function [error] = best_approximation(node,elem.ray,pde)
%% function to compute the best approximation in the ray-FEM space

N 
snode = node(elem(end,:),:);
sray = ray(elem(end,:),:);

[~, err1,~,err2] = best_approximation_in_one_element(node(elem(end,:),:),sray,omega,pde,3);
bb(i) = err1;cc(i) = err2;
end
showrate(aa,bb)
showrate(aa,cc)



